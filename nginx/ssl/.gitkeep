# SSL Certificate Placeholder
# 
# This directory holds SSL certificates for production deployment.
# Certificates are generated by Certbot (Let's Encrypt) and should NOT be committed to git.
#
# Setup:
#   certbot certonly --webroot -w /var/www/gigpay-pwa -d gigpay.in -d api.gigpay.in
#
# Expected files:
#   live/gigpay.in/fullchain.pem
#   live/gigpay.in/privkey.pem
